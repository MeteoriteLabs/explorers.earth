import { memo } from "react";
import PlaceDetails from "../../features/PublicHome/components/PlaceDetails/PlaceDetails";

const PlaceDetailsPage = memo(() => {
  return <PlaceDetails />;
});

export default PlaceDetailsPage;
