# This file is intentionally non-runnable.
x-explorers-compose-authority:
  status: superseded
  production: ../docker-compose.yml
  disposable: ../docker-compose.music-test.yml
  reason: Root Compose owns production and music-test owns disposable rehearsal.

services: {}
